app: one_password
app: 1password
os: mac
-
settings():
    # milliseconds
    key_wait = 25

password copy:
    key(cmd-shift-c)
user name copy:
    key(cmd-ctrl-c)
one time copy:
    key(cmd-ctrl-shift-c)